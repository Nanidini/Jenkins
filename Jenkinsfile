pipeline {
    agent any

    stages {
        stage('Listfiles') {
            steps {
                    sh 'print1.sh "https://github.com/Nanidini/Jenkins.git"' ....
            }
        }
    }
}
