pipeline {
    agent any

    stages {
        stage('Listfiles') {
            steps {
                    sh 'print.sh "https://github.com/Nanidini/Jenkins.git" "C:\Users\navaa\.jenkins\jenkins"'
            }
        }
    }
}
